# 网站视觉方式：方向性叙事与边缘标签

## 核心意图

网站应呈现为一张可阅读、可逐层进入的策展图纸，而不是传统的营销型网页。参考视觉以「楼层索引 / 建筑剖面 / 操作说明」为组织语言：内容沿一条清晰的方向性结构展开，主叙事、条目、注释与交互提示并置；纯白页面、大留白、局部规则线和少量彩色标签共同构成克制而有研究感的节奏。

整体气质：编辑设计、档案感、技术手册、展览导览。冷静、精确、轻盈；避免商业化卡片、过度圆角、厚重阴影和装饰性渐变。

## 版式系统

### 方向性结构

- 每个长页面建立清晰的阅读方向和层级关系。它可通过垂直序列、横向分段、侧边目录、章节编号、时间线、连续图像或滚动节奏表达。
- 刻度、短线、日期与楼层编号只是可选语言，并非固定模板；若使用，必须对应真实内容，不作纯装饰。
- 内容按层级自上而下堆叠：总题 / 导言 → 单元或作品 → 图像、图示与资料 → 注释和链接。
- 桌面端优先保持多列并置；移动端收束为单列，但要保留章节定位与阅读进程，不能变成无结构的信息流。
- 资源总览与类别详情沿用首页主列：主列保持全宽，内容左右使用首页的视觉内边距，移动端再收束为 1rem，避免内容贴近侧边色签。
- 分割线后的首个文本组至少保留约 24px 的垂直留白；桌面端可随容器放大到 32–40px，避免文字贴住分割线。

### 三列阅读关系

默认桌面网格采用不等宽三列，可随页面调整：

| 区域 | 建议比例 | 内容 |
| --- | ---: | --- |
| 索引列 | 1–2 | 页码、章节、状态标签、窄色条或定位线索 |
| 主内容列 | 5–7 | 标题、叙事正文、作品图像或主要交互 |
| 注释列 | 2–3 | 术语、来源、作者、操作提示、延伸阅读 |

- 让相关信息横向对齐，例如：作品图、条目标题和注释共用同一基线。
- 规则线只服务于表格、日历、图示对齐和交互焦点；不使用整宽横线或外框强行切割章节。
- 以大面积留白组织优先级。信息密度可以高，但不能拥挤。

### 局部图纸与图示

- 复杂内容优先用简洁的线框图、编号、尺寸线、箭头、剖面和图例解释。
- 图示采用黑线、白底、灰阶；强调色只标记状态、分类或可操作点。
- 图像与文字不是“配图关系”，而是并列的证据：图像可占满一个网格单元，文字在邻列进行阐释。
- 作品列表可做成“层板 / 目录”式连续结构，每一项像可进入的档案槽位。
- 视觉资料若以网页为主要来源，应使用带来源、域名和明确外链动作的网页资源卡片，不将外部图像复制为网站静态资源；网页资源与文章、视频、工具保持同一层级的目录关系。
- 网页卡片可使用已有网页预览图作为视觉证据，固定预览比例并声明替代文本；预览图只承担识别作用，不替代原网页入口，首屏外预览图应延迟加载。网站地址只保留一个位置，标签与卡片内容保持同一视觉容器。
- 资源详情页的筛选可按“类型 / 标签”分组；当前项沿用对应边缘色签的颜色，标签按钮保持紧凑比例，并通过悬停、聚焦与按下状态传达可操作性。
- 多行筛选栏的总标签、分组标签与第一行选项必须共享基线；桌面筛选项保持紧凑高度，下划线贴近文字，移动端再恢复触控所需的高度。
- 网页详情页可将网页色签作为沉浸式底色，网页卡片保持纸张白色以维持预览与文字的阅读对比；其他媒介详情页继续使用白底。
- 沉浸式网页底色启用时，站点导航改用正文墨色；筛选当前项以对应色签的下划线表达，若色签与底色同色则改用高对比墨色，网页标签悬停只加粗边线，不额外铺设填充色。

## 字体与文字

- 全站统一使用非衬线字体；正文、标题、标签、编号、数据和导航使用同一字体家族，通过字重、字号、字距和留白建立层级。
- 超大标题保持清晰的字间距与呼吸感，避免负字距和过窄的行高；中文标题建议 `letter-spacing: 0–0.03em`、`line-height: 1.05–1.15`。
- 建议正文 16–18px、行高 1.6–1.8；注释 12–14px、行高 1.45–1.6；导航与编号 11–13px，适度字距。
- 段落保持窄而长的书页感。长正文的单行宽度宜控制在约 28–38 个汉字，或 55–75 个拉丁字符。

## 色彩与状态

### 基础色

- 底色：纯白 `#FFFFFF`，保证抠图素材与页面边缘自然融合。
- 主文字与线条：近黑 `#111111`。
- 辅助文字和弱分隔：中灰 `#8C8C88`、浅灰 `#D7D7D1`。

### 强调色

仅将颜色用作可读的分类编码或状态提示。推荐从下列明亮但不过饱和的色组中选取，并在全站固定语义：

- 蓝 `#78A2ED`
- 黄 `#F2EF78`
- 橙红 `#F05A2A`
- 绿 `#2FAF87`
- 洋红 `#DE67B5`
- 米灰 `#D8D5BD`

- 单个屏幕内通常不超过 2–3 个强调色；大部分界面保持黑、白、灰。
- 每种颜色必须有固定含义（例如课程单元、作品媒介、开放状态），并以文字 / 图标 / 标签双重传达，不能只靠颜色。
- 可将窄色签置于页面边缘或条目旁，模拟参考中的侧边索引；颜色只承担分类、导航或状态语义，不铺成大面积背景。

## 组件准则

### 导航与目录

- 桌面导航优先使用贴边的窄色签组：色签组从页面上方约 4vh 处开始，占视口约 2/3 高度；默认是色块，章节名称挂在色签右侧，保持横排后旋转 90°，与色块边缘紧挨并垂直居中；当前项、悬停项或焦点项通过明度、描边或焦点环提示状态，未选中项使用灰色文字。色签不重复显示 `0.0 / 1.0 / 2.0` 序号。
- 章节编号若承担内容层级，应跟随正文标题呈现；网站级导航（首页、课程详细、成果展示）放在页面顶部，以真实链接承担跨页面定位，不使用宽文字列表、厚实胶囊按钮或整条侧栏底色。
- 色签轨道与正文之间保留明确的安全留白；色签可以贴边，但不能贴住正文列。
- 长页面可提供固定章节索引；点击后平滑定位到相应的章节锚点。

### 链接与按钮

- 文本链接优先使用下划线、箭头或括号标记，例如 `阅读资料 →`。
- 按钮保持方正或极小圆角；边框 1px，悬停时反转黑白或显现对应分类色。
- 操作词应明确且短：`查看`、`进入`、`展开注释`、`返回目录`。

### 图例、标签与注释

- 每一种标记方式在首次出现处给出小型图例。
- 注释采用较小字号，放入专属列或图像下方；通过编号与主文关联。
- 使用 `(01)`、`[A]`、`1/9` 等稳定编号体系，便于在页面内跳转和跨页面引用。

### 动效与交互

- 动效应帮助理解方向和定位：章节状态切换、色条变化、图示展开、目录定位。
- 时长建议 160–260ms，缓动克制；避免弹跳、漂浮、无意义的视差与大范围滚动特效。
- 鼠标悬停可显示术语说明、图例对应项或细微的反色；所有悬停信息必须可通过键盘和触摸获得。

## 响应式规则

- 桌面（≥ 1024px）：使用三列或多列并置；贴边色签在滚动中保持可见，并以覆盖方式展开，不改变正文宽度。
- 平板（768–1023px）：压缩为索引窄列 + 主内容 / 注释两列；次要图例可下移。
- 手机（< 768px）：隐藏桌面侧边标签，正文使用完整宽度；章节仍通过标题、编号和垂直节奏保持方向感。
- 不在移动端缩小一切来维持桌面图纸；应重排内容，确保阅读方向、正文、链接和交互目标仍清晰可用。

## 禁止事项

- 不使用通用 SaaS 风格：大圆角卡片、深投影、渐变光晕、玻璃拟态、居中大横幅。
- 不用无意义的“装饰线”和随机颜色；每一根功能线、色签、编号都应承担导航、分类、对齐或解释功能。
- 不将全部内容强制居中；优先使用边缘对齐、基线对齐和不对称留白。
- 不以小字号堆砌正文来模仿参考图的信息密度；密度来自层级、网格和并置，而非牺牲可读性。

## 实施检查

完成一个页面前，检查：

1. 是否能一眼识别当前章节与其在全页中的位置？
2. 是否存在贯穿页面的方向性秩序（章节、编号、边缘色签、时间线或其他定位线索）？
3. 每个强调色是否有固定且可解释的含义？
4. 图、文、注释是否通过网格建立了可读关系？
5. 移动端是否仍保留清晰的章节方向感，并保证阅读可用？
