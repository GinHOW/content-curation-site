# 网站协作说明

## 视觉规范优先级

所有涉及界面、组件、版式、颜色、字体、动效或响应式调整的工作，必须先阅读并遵循 [Design.md](./Design.md)。它定义了本网站的视觉语言：方向性叙事、编辑式多列网格、图纸化图示、克制的强调色和档案式信息层级。

## 实施要求

- CSS 必须保持可读的展开格式：每个选择器块单独换行，每条声明单独占一行；多个选择器、属性或媒体查询不得压缩到同一行。新增或修改样式时沿用现有缩进与换行风格，便于审阅和后续维护。
- 新页面必须建立清晰的章节定位与阅读方向：可使用页码、编号、索引、时间线、连续滚动或语义化色条，但应优先与真实内容结构绑定；刻度不是唯一形式。默认采用纯白底、统一非衬线字体和贴边色签，章节文字可贴在色签右侧旋转呈现（色签不重复显示序号，未选中项降灰），章节编号在对应内容标题处保留；跨页面入口使用真实网站链接。
- 桌面端优先通过不等宽网格并置主内容、索引与注释；移动端应重新编排，而不是简单缩放。
- 颜色只能承担分类、状态或导航语义；新增颜色需在页面或数据结构中说明其含义，并提供不依赖颜色的识别方式。
- 优先使用留白、基线对齐、文本链接和简洁图例；细线只用于表格、日历、数据对齐和焦点状态，避免宏观分割线、大圆角卡片、厚阴影、渐变、玻璃拟态和装饰性动效。
- 图像、作品资料与文字说明应作为同等信息单元在网格中关联，不将图像仅视为正文配图。
- 涉及界面的修改完成后，至少在一个代表性桌面宽度与一个代表性手机宽度检查相关页面：阅读顺序、章节 / 索引可见性、注释归属、色彩对比度与键盘可访问性。

## 高效开发原则

默认采用增量工作方式。开始前用 `git status --short` 和 `git diff --name-only` 确认本次相关文件，只读取、修改和验证任务所需的代码及其直接依赖。仓库中已有的未提交改动均视为用户工作，不回退、不覆盖、不顺手整理；发现无关问题时记录即可，除非它直接阻塞当前任务。

将相互关联的小改动合并为一个完整批次后再验证，避免每编辑一个文件就重复构建、截图或运行同一门禁。验证通过后，只有相关代码再次发生变化才重跑对应检查；失败时先修复首个可执行问题，再重跑受影响的检查，无需重复已经通过且输入未变化的步骤。

优先复用现有依赖、缓存、开发服务器和构建产物。`node_modules` 可用时不重复安装依赖；任务未涉及依赖版本时不更新锁文件。不得为了统一风格而格式化全仓库，不重写无关文件，不生成与改动无关的资产、索引或快照。

## 分级验证与门禁

验证范围应与风险和影响面匹配。纯文案、注释或静态数据的小改动，只检查相关页面或数据消费位置；局部组件和样式改动，只检查该组件、直接引用页面及代表性的桌面和手机视口；路由、鉴权、共享状态、公共组件或构建配置改动，需要检查其主要调用链并运行一次 `npm run build`。若只改文档且未影响可执行代码，默认不运行构建。

全量审查、全站遍历和全量回归不是默认门禁。只有改动跨越多个核心模块、修改依赖或锁文件、调整构建与部署配置、涉及数据库迁移或数据结构、改动全局路由或鉴权边界、进行大范围重构，或者用户明确要求时，才扩大到全量验证。执行全量检查前应先说明触发原因，能用代表性路径覆盖风险时不扩大范围。

未改动且未受依赖链影响的文件无需重新读取、重新格式化、重新截图或计算哈希。默认不做全仓文件哈希、逐文件完整性校验或构建前后哈希比对；只有任务本身涉及发布物完整性、缓存失效、文件损坏、安全取证，或既有流水线明确要求时才进行哈希验证，并限定在相关文件或产物范围内。

项目目前没有独立的 lint 或测试脚本，不额外虚构门禁。对可执行代码的默认收尾检查是一次 `npm run build`；若改动可由更小的语法检查、定向页面检查或已有测试覆盖，可先使用更小检查，满足上述高风险条件时再执行构建。构建只需在一个连贯改动批次结束时运行一次。

## 浏览器与视觉检查

只有界面行为、布局、样式、响应式或可访问性发生变化时才启动浏览器验证。检查与改动直接相关的页面和一组代表性视口即可；桌面端默认检查常用宽度，移动端默认检查一个窄屏宽度。除非改动影响全局布局、导航或设计系统，不逐页截图、不遍历全部路由。纯逻辑改动若已有定向验证证据，不强制截图。

视觉检查发现问题后，只复查修复涉及的页面和视口。开发服务器已经运行且代码能热更新时应继续复用，避免为同一批改动反复启动服务或重新构建。

## 操作边界与交付

本地开发、构建和只读检查可直接进行；远程部署、远程数据库迁移、写入生产数据、发送外部消息及其他会改变外部状态的操作，必须由用户明确要求。不得把 `npm run d1:migrate:remote` 当作常规验证命令；需要验证迁移时优先使用本地环境。

任务结束时用简短段落说明改了什么、验证了什么，以及仍未验证的高风险部分。无需罗列每个读取过的文件或重复输出完整命令日志。若受限于环境无法完成某项关键验证，应准确说明限制和剩余风险，不用无关的全量检查替代。
